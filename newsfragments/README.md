# Release note fragments

Add one user-facing fragment for each change that should appear in the next release.
Name it `<issue-or-commit>.<type>.md`, where the supported types are `feature`, `bugfix`, `doc`, and `misc`.
The fragment should contain one concise sentence written for Slopsplorer users.
