Generated files are now recognised from the header a generator writes, which reaches emitted SDK clients that no path convention marks, and a folder of translations is recognised from the languages it holds, so a documentation site that keeps `content/pl` beside `content/zh-cn` reports them as translations rather than as docs.
