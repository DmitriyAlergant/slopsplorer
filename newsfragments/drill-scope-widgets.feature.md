Drilling into a folder now re-roots the whole page: the headline readouts and the proportion bar describe the drilled folder, with a new "of project" readout keeping the global figure in view, and selecting a `.` row now shows that folder's own files as their own subject instead of repeating the folder's panel.
