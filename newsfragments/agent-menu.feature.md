The agent menu now draws each tool's own mark and says whether the tool reported a sign-in, instead of its version, and a tool that is installed but signed out is offered too, so you can ask it and read what it says.
