The install command for the agent skill now copies the skill into both `~/.claude/skills` and `~/.agents/skills` instead of linking one to the other, reads as PowerShell on Windows, and the dialog offers `SKILL.md` in the same preview the scanned files open in.
