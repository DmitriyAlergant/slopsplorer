A pasted commit, such as `slopsplorer f53f4f9eb`, now compares that commit against its parent instead of against the working tree. A named revision such as `origin/main` or `HEAD~5` still measures from there to the working tree, and `<rev>^!` names one commit whatever points at it.
