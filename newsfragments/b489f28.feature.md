`--pr <number>` reviews a pull request: it fetches the change from the remote, so it reaches one whose branch a squash merge deleted, and measures it against the branch it is actually against rather than against the repository default. GitHub and GitLab, through `gh` or `glab`. A pull request URL works as a positional argument.
