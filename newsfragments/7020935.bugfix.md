Flavor detection now reads the JVM, .NET, Pester, and vitest test-naming conventions and the Gradle test source sets, no longer files the code and prose inside a `lang` or `translations` folder as translation catalogues, counts a `.txt` or `.md` fixture as data rather than documentation, and recognises the generated-file marker whatever language wrote it.
